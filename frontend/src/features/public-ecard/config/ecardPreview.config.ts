export const ECARD_ABOUT_TRUNCATE_WORD_COUNT = 32;

export const ECARD_TESTIMONIAL_ROTATE_INTERVAL_MS = 6000;

/** Target scroll speed for the about-section ticker in pixels per second.
 * The animation duration is computed dynamically from the measured content
 * width so the speed stays constant regardless of text length. */
export const ECARD_TICKER_PX_PER_SECOND = 80;
