/** How long a field's attention-drawing highlight (pulsing ring) stays visible
 * after a validation error surfaces — the field's own red border/message
 * persists independently of this, for as long as the field is actually invalid. */
export const FIELD_ERROR_HIGHLIGHT_DURATION_MS = 3500;
