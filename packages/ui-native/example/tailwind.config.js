// NativeWind's Metro loader resolves `tailwind.config` from the project root.
// Re-export the shared @bizvizcards/ui-native config.
module.exports = require("@bizvizcards/ui-native/tailwind.config");
