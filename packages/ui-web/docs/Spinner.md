---
category: Feedback
---

# Spinner

An indeterminate loading indicator. `label` is the screen-reader text (and shows
as muted text below the spinner when `showLabel` is set). Use inside buttons via
the button's own `loading` prop instead of composing this.

```tsx
<Spinner size="lg" label="Reading card…" showLabel />
```
