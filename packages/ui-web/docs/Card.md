---
category: Containers
---

# Card

A surface container — the default frame for grouped content on a screen
(`rounded-box`, hairline border, soft shadow). Pass `onClick` to make the whole
card a tap target; `flush` removes the inner padding for edge-to-edge media or
lists.

```tsx
<Card onClick={() => openLead(lead.id)}>…</Card>
```
