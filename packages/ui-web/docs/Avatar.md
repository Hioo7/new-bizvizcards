---
category: Data display
---

# Avatar

A circular user/business avatar. Pass `name` (used for the alt text and the
initials fallback) and an optional `src`. Sizes: `sm` | `md` | `lg`.

```tsx
<Avatar name="Chitra Narayan" src={lead.photoUrl} size="lg" />
```
