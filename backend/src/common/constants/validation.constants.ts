/** Generic top-level message for a failed request-body/query validation — field-specific
 * detail lives in the accompanying `fieldErrors` array, see zod-error-formatter.ts. */
export const VALIDATION_FAILED_MESSAGE = 'Validation failed';

export const HTTP_BAD_REQUEST_ERROR_LABEL = 'Bad Request';
